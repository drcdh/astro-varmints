from setuptools import setup

setup(
    name="astrovarmints",
    version="0.1.0",
    install_requires=[
        'importlib-metadata; python_version < "3.8"',
        "sense-hat >= 2.3.1",
    ],
)
